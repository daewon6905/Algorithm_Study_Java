난이도 : Medium

지뢰찾기 게임 룰을 그대로 구현하는 느낌의 문제

지뢰찾기 게임 룰이 햇갈려서 해맴. 현재 위치에서 8방향에 지뢰가 몇개 있는지를 카운팅을 하고 dfs를 진행함.

여기서 visited 배열을 두면 직관적이지만, 'E'가 아니면 이미 방문을 해서 상태를 변경했기에, board[r][c] != 'E'의 조건 확인으로 visited 배열을 대신할 수 있다.

즉, 'E'가 아니면 중단(return)한다. 

주변 지뢰 개수를 카운팅 후 board[r][c] = 'B' 로 방문 표시 후 다음 재귀 진행