난이도: lvl2

처음봤을떈 간단한 dfs/bfs 문제인줄 알았으나, 시간초과 때문에 set을 활용해 한 네트워크가 여러 열에 걸쳐있는 경우, 누적합을 열 별로
배열에 기록해둔다. 처음에 dfs로 풀었는데, 효율성 테스트에서 런타임 에러가 나서 뭐지 했는데 dfs 재귀로 풀면 최악의 경우 재귀의 깊이가 엄청 깊어서 스택 오버 플로우가 원인이었다.
반면에 큐를 활용한 bfs는 수백만개를 저장해도 지장이 없기 때문에 정답이 나온다. 재귀의 스택 오버플로우는 지금까지 고려를 잘 안했는데, 생각하게 해주는 문제였다.