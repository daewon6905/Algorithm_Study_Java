백트래킹에 행과 열 r,c 두개의 값을 넘기지 않고, idx 하나의 값을 넘겨서
2차원 배열을 1차원 배열처럼 생각. 
NQueens 처럼 행을 기준으로 한줄에 하나만 놓으면서 전진해야한다면 r 행값을 넘기면 되지만,
스도쿠 퍼즐처럼 모든 칸을 고려해한다면(전수 조사), idx 방식이 유리.

행,열 계산식도 외워둘만함. 

int r = idx/n  
int c = idx%n

3x3 박스 안의 값 계산

i는 0~8의 값 9x9 전체

int nr = 3*(r/3) + i/3, int nc = 3*(c/3) + i%3