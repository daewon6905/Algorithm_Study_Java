난이도 : Medium

다익스트라 알고리즘과 동일한 논리인 문제. 다만 평소 최단거리를 구하는 문제가 많다면, 이건 확률의 최대를 구하는 문제.
즉 우선순위 큐가 평소 최소 힙이라면 최대 힙, 값의 갱신 조건식이 반대라는 차이 뿐이다.

또한, 확률값의 자료형이 double이라 리스트에 하나의 배열로 구현하려면 double형 배열이 들어가야해서 인덱스 값을 접근할때 int형으로 강제 형변환이 필요하다는 점에 주의.
별도의 Class로 Node를 구현한다면 이런 형변환은 필요없음.