난이도 : lvl3

전형적인 재귀 활용 dfs 문제 같지만, 가지치기가 매우 중요한 문제.

일부 테스트 케이스가 시간초과 나면서 가지치기에 대해 고민 많이한 문제.

포인트는 재귀를 진행할때 현재 위치에서 도착 지점까지의 맨하튼 거리가 총 k에서 현재까지 진행한 거리를 뺀 값을 비교하여 가지치기를 진행해야한다.

**if(Math.abs((r-sr) + Math.abs(c-sc) > k-sb.length()) return false;**

남은 맨하튼 거리가 남은 이동 수 보다 크다면, 즉시 재귀를 종료.
